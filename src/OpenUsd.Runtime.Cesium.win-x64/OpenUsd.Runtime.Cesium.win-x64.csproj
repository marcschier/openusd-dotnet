<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <PackageId>OpenUsd.Runtime.Cesium.win-x64</PackageId>
    <Description>OpenUSD Cesium 3D Tiles native runtime shim for Windows x64.</Description>
    <PackageTags>openusd;usd;cesium;3d-tiles;native;runtime</PackageTags>
    <RuntimePackageKind>Cesium</RuntimePackageKind>
    <RuntimePackageRid>win-x64</RuntimePackageRid>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <IncludeSymbols>false</IncludeSymbols>
    <!--
      Package validation compares managed lib/ref assemblies. This package suppresses build output and
      ships only native assets and buildTransitive targets, so the opt-out is intentional.
    -->
    <EnablePackageValidation>false</EnablePackageValidation>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
  </PropertyGroup>

  <Import Project="../OpenUsd.Runtime.Packaging/OpenUsd.Runtime.Packaging.targets" />

</Project>
