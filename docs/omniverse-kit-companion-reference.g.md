<!-- generated by eng/generate-kit-companion-spec.py -- do not edit -->

# Omniverse Kit companion -- generated reference tables

Generated from `eng/kit-companion-spec.json`; do not edit by hand. This is the structured reference appendix for
[`docs/omniverse-kit-companion.md`](omniverse-kit-companion.md), which is the hand-authored, implementation-ready
specification. Nothing in either document is evidence of an implementation: the companion extension remains a
separate, not-yet-authorized repository.

## Protocol constants

Bound names below are short member names; `LA.` prefixes `OpenUsd.LiveAuthoring.LiveAuthoringValidation` and `BP.`
prefixes `OpenUsd.Bridge.Protocol.BridgeProtocol`.

| Constant | Value | Source |
| --- | --- | --- |
| Package | `openusd.bridge.v1` | `BP.PackageName` |
| Service | `openusd.bridge.v1.LiveBridge` | `BP.ServiceName` |
| Protocol version | `1.0` | `BP.Version` |
| Max updates per batch | 4,096 | `LA.MaxUpdatesPerBatch` |
| Max collection elements | 65,536 | `LA.MaxCollectionElementCount` |
| Max total collection elements/batch | 4,194,304 | `LA.MaxTotalCollectionElementCountPerBatch` |
| Max estimated batch payload bytes | 16,777,216 | `LA.MaxEstimatedBatchPayloadBytes` |
| Max frame overhead bytes | 65,536 | `BP.MaxFrameBytes` framing allowance |
| Replay window (default / max) | 64 / 4,096 | `LA.DefaultReplayWindowLength` / `MaxReplayWindowLength` |
| Replay ledger entry bytes | 64 | `LA.ReplayLedgerEntryBytes` |

## Server RPC behavior summary

| RPC | Kind | Retry-safe |
| --- | --- | --- |
| `Negotiate` | unary | no |
| `GetSnapshot` | unary | yes |
| `GetStatus` | unary | yes |
| `PublishLocalBatch` | unary | no |
| `StreamChanges` | bidi-stream | no |

## Kit USD edit to wire update mapping

| Authoring type | Wire case |
| --- | --- |
| `DefinePrimUpdate` | `StageUpdate.define_prim` |
| `RemovePrimUpdate` | `StageUpdate.remove_prim` |
| `SetAttributeUpdate` | `StageUpdate.set_attribute` |
| `ClearUpdate` | `StageUpdate.clear` |
| `SetRelationshipTargetsUpdate` | `StageUpdate.set_relationship_targets` |
| `SetReferenceUpdate` | `StageUpdate.set_reference` |
| `SetPayloadUpdate` | `StageUpdate.set_payload` |
| `SetActiveUpdate` | `StageUpdate.set_active` |
| `SetInstanceableUpdate` | `StageUpdate.set_instanceable` |
| `SetVariantSelectionUpdate` | `StageUpdate.set_variant_selection` |
| `SetMetadataUpdate` | `StageUpdate.set_metadata` |
| `ApiSchemaUpdate` | `StageUpdate.api_schema` |
| `SetPointInstancerOrientationsUpdate` | `StageUpdate.set_point_instancer_orientations` |
| `ReplaceBridgeOverlayUpdate` | *(no delta case -- see notes)* |

Notes:

- `SetAttributeUpdate`:
  SetAttribute.time_code unset (SetAttributeUpdate.TimeCode == null) authors the attribute's default value;
  time_code set authors one discrete time sample at that code. Both are the same wire case -- there is no separate
  time-sample message.
- `ApiSchemaUpdate`:
  Requires the CAPABILITY_API_SCHEMA capability to have been agreed during negotiation.
- `SetPointInstancerOrientationsUpdate`:
  Requires the CAPABILITY_POINT_INSTANCER_ORIENTATIONS capability to have been agreed during negotiation.
- `ReplaceBridgeOverlayUpdate`:
  Only representable as a StageSnapshot returned by GetSnapshot or pushed on StreamChanges. Encoding one inside a
  delta throws BridgeProtocolException(OverlayReplacementNotAllowed).

## Kit edits that require a full snapshot

- **Renaming or reparenting a prim inside the bridge-owned overlay**
  The wire model has no rename/move update case; RemovePrimUpdate plus DefinePrimUpdate at a new path would lose the
  identity continuity a rename implies, and could race with in-flight deltas that still reference the old path. The
  companion must send a full StageSnapshot instead.
- **Sublayer, reference, or payload structure changes on the bridge root prim itself**
  As opposed to authoring SetReferenceUpdate/SetPayloadUpdate on a prim under the root: layer-structural edits change
  what composes the overlay, not a value inside it, and are outside the coordinator's single-prim-scoped update model.
  Requires a full snapshot.
- **Restructuring variant sets (adding or removing a variant set or a variant)**
  As opposed to SetVariantSelectionUpdate choosing among existing variants: only variant selection is a wire case.
  Structural variant set changes require a full snapshot.
- **A composition arc or override authored outside bridgeRootPath affecting prims inside it**
  The coordinator only accepts updates whose target path is at or under the bridge root; an edit reaching in from
  outside is bridge-scope-violating by construction and is never partially applied.
- **Removing a single authored time sample in isolation**
  Leaving the default value and every other time sample intact: ClearUpdate's CLEAR_TARGET_ATTRIBUTE_VALUE clears an
  attribute's default value and all of its time samples together; there is no wire case for removing one discrete time
  sample without touching the rest. A companion needing that sends a full StageSnapshot instead.
- **Any edit the server cannot express as a StageUpdate oneof case**
  The oneof is closed and versioned (see updateMapping for the full, cross-checked list); an update kind the current
  major version does not define is never approximated by a different case. It is deferred to a full snapshot and, if
  it recurs, to a proposed wire addition under a new minor version.

## Acceptance matrix

### `round-trip` -- Round-trip authoring

- **Method**:
  A batch authored on the OpenUsd.LiveAuthoring side is published (PublishLocalBatch), observed as a StageDelta
  with the same origin_id on a second independently-negotiated connection, and re-applied there byte-for-byte
  equal to the source batch's updates.
- **Pass criteria**:
  The second connection's applied overlay state is structurally equal to the first connection's local state, and
  the publishing connection never receives its own batch back as a non-echo-suppressed delta.

### `join-leave` -- Session join and leave

- **Method**:
  A client negotiates, receives an initial StageSnapshot, disconnects cleanly (DisposeAsync-equivalent), and a
  second client negotiates fresh against the same server-owned bridge root.
- **Pass criteria**:
  The leaving client's disposal is idempotent and presents no further credential or mutation; the joining client's
  snapshot reflects the full current overlay state with no residue from the first client's session identity or
  epoch.

### `ordered-1000-edits` -- 1000 ordered edits

- **Method**:
  1000 sequentially numbered StageDelta or PublishLocalBatch updates are sent in strict per-epoch sequence order
  under the negotiated MaxUpdatesPerBatch/byte bounds.
- **Pass criteria**:
  Every update is applied exactly once, in order, with no gap, no duplicate application, and the final overlay
  state matches the deterministic result of applying all 1000 updates in the sent order.

### `time-sample-authoring` -- Time-sampled attribute authoring

- **Method**:
  A SetAttributeUpdate is published with SetAttribute.time_code set, authoring a discrete time sample rather than
  the default value; a second SetAttributeUpdate for the same attribute is published with time_code unset,
  authoring the default value; both are read back through GetSnapshot.
- **Pass criteria**:
  The time-sampled write leaves the attribute's default value untouched and adds exactly one time sample at the
  given code; the default-value write does not add or remove any time sample; a snapshot faithfully separates the
  default value from every authored time sample rather than collapsing them.

### `epoch-change-renegotiation` -- Epoch change forces renegotiation, never silent adoption

- **Method**:
  While a client's StreamChanges connection is open, the server's authoritative epoch for the negotiated bridge
  root changes (for example a second Kit session takes over); separately, a client calls GetSnapshot with the
  request epoch it negotiated after the server's authoritative epoch has already advanced past it.
- **Pass criteria**:
  The open connection never receives a Snapshot, Delta, or Acknowledgement frame naming the new epoch: it is
  terminated (or the next frame is rejected with SESSION_REJECTION_EPOCH_ADVANCED) so the client must call
  Negotiate again; GetSnapshot never substitutes a snapshot for a different epoch than the one requested -- it
  fails the call instead. In both cases the client only obtains the new epoch, its capabilities, and its limits
  together through a fresh Negotiate call, followed by a fresh GetSnapshot.

### `reverse-edits` -- Reverse-order / out-of-order delivery

- **Method**:
  A transport redelivers a burst of already-sent sequences out of strict order (for example after a retransmit),
  including at least one sequence the receiver already applied.
- **Pass criteria**:
  A sequence lower than the last accepted sequence is treated as SESSION_OUTCOME_DUPLICATE (identical content,
  answered with a freshly formed acknowledgement rather than the original SESSION_OUTCOME_APPLIED response) or
  SESSION_REJECTION_DUPLICATE_CONFLICT (differing content) per the idempotency ledger rules; the overlay is never
  mutated a second time by a re-delivered identical sequence, and a genuine conflict forces a full resync rather
  than an inconsistent partial apply.

### `backpressure` -- Backpressure under a slow consumer

- **Method**:
  The server (or the client's bounded outbound queue, for local-edit export) is driven past its documented bounded
  buffer depth by a producer or change rate the consumer cannot keep up with.
- **Pass criteria**:
  The bounded queue refuses admission (never grows unbounded) or the server emits ResyncRequired; no update is
  silently dropped without either an explicit rejection outcome or a resync request that lets the peer recover a
  consistent baseline.

### `restart-resync` -- Restart and full resync

- **Method**:
  The server process (or the client) is restarted mid-session, and the surviving peer reconnects.
- **Pass criteria**:
  The reconnecting peer negotiates a new or resumed epoch, requests and applies a full StageSnapshot before
  accepting any StageDelta, and never applies a delta numbered against a stale, pre-restart epoch.

### `auth-version-mismatch` -- Authentication and protocol-version mismatch

- **Method**:
  A client presents an invalid or missing credential, and separately, a client whose major protocol version
  differs from the server's is negotiated against.
- **Pass criteria**:
  An invalid/missing credential is rejected with HANDSHAKE_REJECTION_UNAUTHENTICATED before any capability or
  limit is evaluated; a major-version mismatch is rejected with HANDSHAKE_REJECTION_VERSION before capabilities or
  limits are evaluated; neither rejection ever attempts a mutation first.

### `independent-upgrades` -- Independent upgrade of client and server

- **Method**:
  The client is upgraded to a newer OpenUsd.Bridge.Protocol/Grpc minor version while the server stays on an older
  compatible minor version, and vice versa.
- **Pass criteria**:
  Negotiation succeeds on major-version equality alone; the newer side's additional capabilities are simply absent
  from the agreed set when the older side does not advertise them, and no additional-capability update is sent or
  accepted by the side that does not support it.

### `license-isolation` -- License and dependency isolation

- **Method**:
  Building and testing this repository's OpenUsd.Bridge.Protocol/OpenUsd.Bridge.Grpc packages in isolation,
  without the companion repository present or referenced anywhere in this repository's source, build files, or
  restored packages.
- **Pass criteria**:
  Both packages restore, build, and test with no NVIDIA-licensed dependency, no reference to a companion
  repository, and no package manifest requiring one; tests/OpenUsd.Package.Tests/BridgePackageTests.cs continues
  to assert this, and this specification introduces nothing that would require it to change.

## Installation channel status

| Channel | Status |
| --- | --- |
| `ngc` | not-yet-published |
| `extension-manager` | not-yet-published |
| `local-development` | not-yet-published |

## Example artifacts

- [`examples/omniverse-kit-companion/extension.toml`](examples/omniverse-kit-companion/extension.toml)
  Not a shipped, buildable, or installable Kit extension manifest. Every version field is a literal 'pending'
  placeholder tied to eng/omniverse-profile.json's kit-baseline dependency; this file cannot be pointed at by Kit's
  Extension Manager or an extension search path and produce a working extension.
- [`examples/omniverse-kit-companion/companion_service.py`](examples/omniverse-kit-companion/companion_service.py)
  Not a runnable module: it imports a placeholder generated-stub package that does not exist, and its docstring and
  file extension are chosen so it cannot be mistaken for source under a build system's discovery path. It illustrates
  serverBehavior above in code shape only.

## Related documents

- [Omniverse bridge protocol and client](omniverse-bridge.md)
- [Omniverse interoperability profile](omniverse-profile.md)
- [Live authoring](live-authoring.md)
- [Omniverse Kit companion specification](omniverse-kit-companion.md)
- [eng/omniverse-profile.json](../eng/omniverse-profile.json)
- [eng/kit-companion-spec.schema.json](../eng/kit-companion-spec.schema.json)

---

_This document is regenerated by `python eng/generate-kit-companion-spec.py`._
_Run without `--verify` to update it after editing `eng/kit-companion-spec.json`._
