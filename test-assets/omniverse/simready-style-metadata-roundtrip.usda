#usda 1.0
(
    "This is a synthetic, repository-authored, MIT-licensed fixture. SyntheticSimReadyMetadata below is a clearly labeled placeholder namespace loosely modeled on the publicly documented pattern of a vendor-namespace dictionary nested under customLayerData/customData (see e.g. the SimReady Foundation's open specification). It does not implement the real SimReady schema, copy any validator logic, or claim vendor compliance."
    customLayerData = {
        dictionary SyntheticSimReadyMetadata = {
            string assetType = "synthetic-fixture-widget"
            string source = "openusd2-repository"
            string physicsProfile = "neutral"
        }
    }
)

def Xform "World"
{
    def Xform "Prop" (
        customData = {
            dictionary SyntheticSimReadyMetadata = {
                string semanticLabel = "prop"
            }
        }
    )
    {
    }
}
